# i18n A1+A3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Internationalize the customer-facing surface of modestshop.md across RO / RU / EN with path-prefixed routing and per-page SEO meta.

**Architecture:** `react-i18next` + JSON namespaces, path-prefixed routes (`/:lang/*`), single `<LanguageGate>` validates and applies the locale. RO is the source of truth; RU and EN translated from RO preserving brand voice. SEO meta via `react-helmet-async` with full `hreflang` set.

**Tech Stack:** React 18, TypeScript, Vite, react-router-dom v7, i18next, react-i18next, react-helmet-async.

**Spec:** `docs/superpowers/specs/2026-05-02-i18n-a1-a3-design.md`

**Environment notes:**
- No test runner. Verification = `npm run build`.
- ESLint pre-broken (extends `next/core-web-vitals` on a Vite project) — skip `npm run lint`.
- Not a git repo. No commits.
- Brand spine: **"Eleganta nu se striga"** ("Elegance doesn't shout"). All translations stay restrained, declarative, never breathless. RU and EN match RO tone precisely.

---

## Task 1: Foundation — deps, i18n config, routing, helpers

**Files (create):**
- `src/i18n/index.ts`
- `src/i18n/locales/ro/common.json` (empty placeholder, populated in Task 2)
- `src/i18n/locales/ru/common.json` (empty placeholder, populated in Task 2)
- `src/i18n/locales/en/common.json` (empty placeholder, populated in Task 2)
- `src/components/LanguageGate.tsx`
- `src/hooks/useLocalizedHref.ts`

**Files (modify):**
- `package.json` — add deps
- `src/main.tsx` — import i18n, wrap in HelmetProvider
- `src/App.tsx` — wrap routes with `/:lang/*`, add bare-root redirect

- [ ] **Step 1: Install dependencies**

Run from `/Users/bigjeery/Documents/wrk/fragrance-shop-main`:

```bash
npm install i18next react-i18next react-helmet-async
```

Expected: install succeeds. `i18next`, `react-i18next`, `react-helmet-async` appear in `package.json` dependencies.

- [ ] **Step 2: Create i18n config**

Write `src/i18n/index.ts`:

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

import roCommon from './locales/ro/common.json';
import ruCommon from './locales/ru/common.json';
import enCommon from './locales/en/common.json';

export const SUPPORTED_LANGS = ['ro', 'ru', 'en'] as const;
export type SupportedLang = (typeof SUPPORTED_LANGS)[number];

export const DEFAULT_LANG: SupportedLang = 'ro';

export function isSupportedLang(value: string | undefined): value is SupportedLang {
  return value !== undefined && (SUPPORTED_LANGS as readonly string[]).includes(value);
}

i18n.use(initReactI18next).init({
  resources: {
    ro: { common: roCommon },
    ru: { common: ruCommon },
    en: { common: enCommon },
  },
  lng: DEFAULT_LANG,
  fallbackLng: DEFAULT_LANG,
  defaultNS: 'common',
  ns: ['common'],
  interpolation: {
    escapeValue: false,
  },
  returnNull: false,
});

export default i18n;
```

Note: subsequent tasks will add namespace imports here (`home`, `shop`, `product`, `discovery`, `checkout`, `order`, `static`) and append them to `resources` and `ns`. Each task that introduces a new namespace MUST modify this file accordingly.

- [ ] **Step 3: Create empty locale namespace files**

Write each of `src/i18n/locales/ro/common.json`, `src/i18n/locales/ru/common.json`, `src/i18n/locales/en/common.json` with:

```json
{}
```

- [ ] **Step 4: Create useLocalizedHref hook**

Write `src/hooks/useLocalizedHref.ts`:

```ts
import { useTranslation } from 'react-i18next';
import { DEFAULT_LANG, isSupportedLang } from '@/i18n';

export function useLocalizedHref() {
  const { i18n } = useTranslation();
  const lang = isSupportedLang(i18n.language) ? i18n.language : DEFAULT_LANG;
  return (path: string) => {
    const normalized = path.startsWith('/') ? path : `/${path}`;
    return `/${lang}${normalized}`;
  };
}
```

- [ ] **Step 5: Create LanguageGate component**

Write `src/components/LanguageGate.tsx`:

```tsx
import { useEffect } from 'react';
import { Navigate, Outlet, useLocation, useParams } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { DEFAULT_LANG, isSupportedLang } from '@/i18n';

export function LanguageGate() {
  const { lang } = useParams<{ lang: string }>();
  const location = useLocation();
  const { i18n } = useTranslation();

  const valid = isSupportedLang(lang);

  useEffect(() => {
    if (!valid) return;
    if (i18n.language !== lang) {
      i18n.changeLanguage(lang);
    }
    document.documentElement.lang = lang;
  }, [lang, valid, i18n]);

  if (!valid) {
    const rest = location.pathname.replace(/^\/[^/]+/, '');
    const target = `/${DEFAULT_LANG}${rest}${location.search}${location.hash}`;
    return <Navigate to={target} replace />;
  }

  return <Outlet />;
}
```

- [ ] **Step 6: Wire HelmetProvider and i18n in main.tsx**

Read `src/main.tsx`. Add the imports and wrap the app:

```tsx
import { HelmetProvider } from 'react-helmet-async';
import './i18n';

// ... existing imports

createRoot(document.getElementById('root')!).render(
  <HelmetProvider>
    <App />
  </HelmetProvider>,
);
```

Preserve any existing wrappers (e.g., StrictMode) outside or inside HelmetProvider as the existing structure dictates.

- [ ] **Step 7: Wrap routes in App.tsx**

Read `src/App.tsx`. Restructure the route tree so all existing routes sit under a `/:lang/*` parent that renders `<LanguageGate>`. Add a bare-root redirect.

Pattern:

```tsx
import { LanguageGate } from '@/components/LanguageGate';

// inside <BrowserRouter><Routes>:
<Routes>
  <Route path="/" element={<Navigate to="/ro" replace />} />
  <Route path="/:lang" element={<LanguageGate />}>
    <Route index element={<Index />} />
    <Route path="shop" element={<Shop />} />
    <Route path="product/:id" element={<Product />} />
    <Route path="discovery-sets" element={<DiscoverySets />} />
    <Route path="discovery-set/:id" element={<DiscoverySet />} />
    <Route path="checkout" element={<Checkout />} />
    <Route path="orders/:orderId" element={<OrderConfirmation />} />
    <Route path="about" element={<About />} />
    <Route path="contact" element={<Contact />} />
    <Route path="faq" element={<FAQ />} />
    <Route path="privacy" element={<Privacy />} />
    <Route path="terms" element={<Terms />} />
    <Route path="careers" element={<Careers />} />
    <Route path="login" element={<Login />} />
    <Route path="admin/*" element={<ProtectedRoute><Admin /></ProtectedRoute>} />
    <Route path="admin/orders/:orderId" element={<ProtectedRoute><AdminOrder /></ProtectedRoute>} />
    <Route path="*" element={<NotFound />} />
  </Route>
</Routes>
```

Preserve all existing `lazy()` imports, `<Suspense>` wrappers, `<ScrollToTop />`, `<ProtectedRoute>` gating, and any `<Toaster>` mounts. Only the route shape changes.

- [ ] **Step 8: Verify build**

Run: `npm run build`
Expected: success.

If TypeScript complains about missing JSON module declarations, add to `src/vite-env.d.ts`:

```ts
declare module '*.json' {
  const value: Record<string, unknown>;
  export default value;
}
```

(If `tsconfig.json` already has `resolveJsonModule: true`, no declaration is needed.)

- [ ] **Step 9: Manual smoke check**

Run `npm run dev`. Visit:
- `http://localhost:8080/` → redirects to `/ro`
- `http://localhost:8080/ro` → Home renders
- `http://localhost:8080/xyz` → redirects to `/ro/xyz` then 404 (NotFound)
- `http://localhost:8080/en/shop` → Shop renders, `<html lang="en">` in DOM

If any redirect or render fails, fix before proceeding.

---

## Task 2: `common` namespace + chrome (Header, Footer, CartSheet, SearchOverlay, BrandWall, toasts) + LanguageSwitcher

**Files (create):**
- `src/components/LanguageSwitcher.tsx`

**Files (modify):**
- `src/i18n/locales/ro/common.json`
- `src/i18n/locales/ru/common.json`
- `src/i18n/locales/en/common.json`
- `src/components/Header.tsx`
- `src/components/Footer.tsx`
- `src/components/CartSheet.tsx`
- `src/components/SearchOverlay.tsx`
- `src/components/BrandWall.tsx`
- Any utility hook that surfaces toasts with hardcoded RO strings (search for `toast(` and `toast.success(`)

- [ ] **Step 1: Read every chrome file and inventory hardcoded strings**

Read each file in the modify list. List every hardcoded RO string that appears as text content, `aria-label`, `placeholder`, button label, toast message, or empty-state message. Group by purpose.

- [ ] **Step 2: Populate `ro/common.json` with the inventoried keys**

Use a flat, two-level-max key structure organized by area. Skeleton (extend with whatever the inventory surfaces):

```json
{
  "header": {
    "menu": "Meniu",
    "search": "Caută",
    "cart": "Coș",
    "shop": "Magazin",
    "discoverySets": "Seturi de descoperire",
    "about": "Despre",
    "contact": "Contact",
    "login": "Autentificare",
    "account": "Cont"
  },
  "footer": {
    "navigation": "Navigare",
    "support": "Suport",
    "legal": "Legal",
    "shop": "Magazin",
    "discoverySets": "Seturi de descoperire",
    "faq": "Întrebări frecvente",
    "contact": "Contact",
    "about": "Despre",
    "privacy": "Politica de confidențialitate",
    "terms": "Termeni și condiții",
    "careers": "Cariere",
    "rights": "Toate drepturile rezervate."
  },
  "cart": {
    "title": "Coșul tău",
    "empty": "Coșul este gol",
    "emptyHint": "Adaugă produse pentru a începe.",
    "subtotal": "Subtotal",
    "checkout": "Finalizare comandă",
    "continueShopping": "Continuă cumpărăturile",
    "remove": "Șterge",
    "quantity": "Cantitate",
    "decanted": "decant"
  },
  "search": {
    "placeholder": "Caută parfumuri, branduri, note...",
    "empty": "Niciun rezultat",
    "emptyHint": "Încearcă alți termeni.",
    "recent": "Căutări recente",
    "close": "Închide"
  },
  "brandWall": {
    "title": "Branduri reprezentate"
  },
  "toast": {
    "addedToCart": "Adăugat în coș",
    "removedFromCart": "Șters din coș",
    "linkCopied": "Link copiat",
    "errorGeneric": "A apărut o eroare. Încearcă din nou."
  },
  "languageSwitcher": {
    "label": "Limbă",
    "ro": "RO",
    "ru": "RU",
    "en": "EN"
  },
  "countries": {
    "MD": "Moldova",
    "AT": "Austria",
    "BE": "Belgia",
    "BG": "Bulgaria",
    "HR": "Croația",
    "CY": "Cipru",
    "CZ": "Cehia",
    "DK": "Danemarca",
    "EE": "Estonia",
    "FI": "Finlanda",
    "FR": "Franța",
    "DE": "Germania",
    "GR": "Grecia",
    "HU": "Ungaria",
    "IE": "Irlanda",
    "IT": "Italia",
    "LV": "Letonia",
    "LT": "Lituania",
    "LU": "Luxemburg",
    "MT": "Malta",
    "NL": "Olanda",
    "PL": "Polonia",
    "PT": "Portugalia",
    "RO": "România",
    "SK": "Slovacia",
    "SI": "Slovenia",
    "ES": "Spania",
    "SE": "Suedia"
  }
}
```

Add any keys the inventory revealed that aren't in this skeleton. Remove any keys whose strings don't actually appear in the chrome files.

- [ ] **Step 3: Write `ru/common.json` — translate from RO**

Translate every key value in `ro/common.json` to Russian. Preserve the exact key structure. Brand voice: restrained, declarative, no marketing flourish. Country names use standard Russian forms (e.g., `"FR": "Франция"`, `"DE": "Германия"`).

- [ ] **Step 4: Write `en/common.json` — translate from RO**

Translate every key value in `ro/common.json` to English. Preserve the exact key structure. Brand voice: quiet, declarative. Country names in standard English (e.g., `"FR": "France"`, `"DE": "Germany"`).

- [ ] **Step 5: Register `common` namespace properly in i18n config**

The `common` namespace was already registered in Task 1. Verify `src/i18n/index.ts` imports the JSON files from all three locales and includes them in `resources`. No change needed if Task 1 was completed correctly.

- [ ] **Step 6: Create LanguageSwitcher component**

Write `src/components/LanguageSwitcher.tsx`:

```tsx
import { useLocation, useNavigate } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { SUPPORTED_LANGS, type SupportedLang } from '@/i18n';
import { cn } from '@/lib/utils';

interface LanguageSwitcherProps {
  variant: 'header' | 'footer';
  className?: string;
}

export function LanguageSwitcher({ variant, className }: LanguageSwitcherProps) {
  const { i18n, t } = useTranslation('common');
  const location = useLocation();
  const navigate = useNavigate();
  const current = i18n.language as SupportedLang;

  const handleSwitch = (lang: SupportedLang) => {
    if (lang === current) return;
    const rest = location.pathname.replace(/^\/[^/]+/, '');
    const target = `/${lang}${rest}${location.search}${location.hash}`;
    navigate(target);
  };

  const sizeClass = variant === 'header' ? 'text-caption' : 'text-body';

  return (
    <div
      role="group"
      aria-label={t('languageSwitcher.label')}
      className={cn('flex items-center gap-2', className)}
    >
      {SUPPORTED_LANGS.map((lang, idx) => (
        <span key={lang} className="flex items-center gap-2">
          <button
            type="button"
            onClick={() => handleSwitch(lang)}
            className={cn(
              sizeClass,
              'transition-colors duration-quick ease-default',
              lang === current ? 'text-strong font-medium' : 'text-muted hover:text-text',
            )}
            aria-current={lang === current ? 'true' : undefined}
          >
            {t(`languageSwitcher.${lang}`)}
          </button>
          {idx < SUPPORTED_LANGS.length - 1 && (
            <span className="text-faint" aria-hidden="true">·</span>
          )}
        </span>
      ))}
    </div>
  );
}
```

- [ ] **Step 7: Migrate Header.tsx**

Read `src/components/Header.tsx`. For every hardcoded RO string, replace with `t('header.<key>')` after adding `const { t } = useTranslation('common');`. For every `<Link to="/...">` and `navigate('/...')` call, switch to `useLocalizedHref()`:

```tsx
import { useTranslation } from 'react-i18next';
import { useLocalizedHref } from '@/hooks/useLocalizedHref';
import { LanguageSwitcher } from '@/components/LanguageSwitcher';

const { t } = useTranslation('common');
const href = useLocalizedHref();

// before: <Link to="/shop">Magazin</Link>
// after:  <Link to={href('/shop')}>{t('header.shop')}</Link>
```

Mount `<LanguageSwitcher variant="header" />` in the desktop header right cluster between the search trigger and the cart trigger. On mobile, place it as the first row inside the menu sheet.

- [ ] **Step 8: Migrate Footer.tsx**

Read `src/components/Footer.tsx`. Same pattern: `useTranslation('common')`, `useLocalizedHref()`. Replace section headings, link labels, and the rights line. Mount `<LanguageSwitcher variant="footer" />` in the right column above the legal links.

- [ ] **Step 9: Migrate CartSheet.tsx**

Read `src/components/CartSheet.tsx`. Replace hardcoded strings with `t('cart.<key>')`. Update the "Finalizare comandă" / checkout link to `useLocalizedHref()`. Update any `navigate('/checkout')` calls.

- [ ] **Step 10: Migrate SearchOverlay.tsx**

Read `src/components/SearchOverlay.tsx`. Replace placeholder, empty state, and recent-searches label with `t('search.<key>')`. Update product result links to `useLocalizedHref()`.

- [ ] **Step 11: Migrate BrandWall.tsx**

Read `src/components/BrandWall.tsx`. Replace heading with `t('brandWall.title')`. Update any brand-detail links if present.

- [ ] **Step 12: Migrate toast call sites**

Run from project root:

```bash
grep -rn "toast(" src/ --include="*.ts" --include="*.tsx"
grep -rn "toast.success\|toast.error\|toast.warning" src/ --include="*.ts" --include="*.tsx"
```

For every customer-facing call site (i.e., NOT in `src/components/admin/`, NOT in `src/pages/Admin*.tsx`), replace the hardcoded RO message with `t('toast.<key>')` after adding `useTranslation` to the surrounding component or hook. If a hook can't directly use the hook (called outside a React component), accept the existing string for now and note it as deferred — admin-adjacent toasts stay RO, and pure utility toasts that fire in non-React contexts are vanishingly rare in this codebase.

- [ ] **Step 13: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 14: Smoke test**

Run `npm run dev`. Visit:
- `/ro` → header in Romanian, language switcher visible
- Click `EN` in header → URL becomes `/en`, header strings switch to English
- Open cart sheet on `/en/shop` → cart strings in English
- Open search overlay → placeholder in active language
- Footer language switcher mirrors header behavior

---

## Task 3: `home` namespace + Index, DiscoveryCTA, NewArrivalsCarousel

**Files (create):**
- `src/i18n/locales/ro/home.json`
- `src/i18n/locales/ru/home.json`
- `src/i18n/locales/en/home.json`

**Files (modify):**
- `src/i18n/index.ts` — register new namespace
- `src/pages/Index.tsx`
- `src/components/home/DiscoveryCTA.tsx`
- `src/components/NewArrivalsCarousel.tsx`

- [ ] **Step 1: Inventory strings**

Read all three component files. List hardcoded RO strings: hero tagline, hero subhead, hero CTAs, section titles/subtitles, carousel empty states, DiscoveryCTA copy.

- [ ] **Step 2: Populate `ro/home.json`**

Skeleton — extend or trim per inventory:

```json
{
  "hero": {
    "tagline": "Eleganta nu se striga",
    "subhead": "Pentru cei care lasa parfumul sa vorbeasca",
    "ctaShop": "Vezi magazinul",
    "ctaDiscovery": "Seturi de descoperire"
  },
  "newArrivals": {
    "title": "Noutăți",
    "subtitle": "Ultimele intrări în colecție",
    "viewAll": "Vezi toate"
  },
  "discoveryCta": {
    "title": "Descoperă fără să te angajezi",
    "body": "Construiește un set de decanturi de 2ml. Încearcă șapte parfumuri înainte să alegi unul.",
    "cta": "Configurează setul"
  }
}
```

- [ ] **Step 3: Translate to `ru/home.json` and `en/home.json`**

Same brand voice: restrained, no exclamation marks, no marketing-speak. The hero tagline can stay in Romanian across all three locales as a brand mark (decision: keep `"Eleganta nu se striga"` literal in RO/RU/EN — it's a brand line, not body copy). Subhead translates.

Suggested translations for the tagline approach:
- RU `hero.tagline`: `"Eleganta nu se striga"` (kept as brand mark)
- EN `hero.tagline`: `"Eleganta nu se striga"` (kept as brand mark)

If on review the user wants the tagline translated, swap in localized versions and document in a follow-up commit. For this pass, keep it as a brand mark.

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Edit `src/i18n/index.ts`:

```ts
import roHome from './locales/ro/home.json';
import ruHome from './locales/ru/home.json';
import enHome from './locales/en/home.json';

// inside resources:
ro: { common: roCommon, home: roHome },
ru: { common: ruCommon, home: ruHome },
en: { common: enCommon, home: enHome },

// inside ns:
ns: ['common', 'home'],
```

- [ ] **Step 5: Migrate Index.tsx**

Read `src/pages/Index.tsx`. Replace hero tagline, subhead, CTAs with `t('hero.<key>', { ns: 'home' })` (or `useTranslation('home')` at top, then `t('hero.tagline')`). Replace section titles with the same. Migrate all `<Link to="/...">` and `navigate('/...')` calls to `useLocalizedHref()`.

- [ ] **Step 6: Migrate DiscoveryCTA.tsx**

Read `src/components/home/DiscoveryCTA.tsx`. `useTranslation('home')`. Replace title/body/CTA with `t('discoveryCta.<key>')`. Migrate the CTA link via `useLocalizedHref()`.

- [ ] **Step 7: Migrate NewArrivalsCarousel.tsx**

Read `src/components/NewArrivalsCarousel.tsx`. Replace title/subtitle/viewAll with `t('newArrivals.<key>')` from the `home` namespace.

- [ ] **Step 8: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 9: Smoke test**

Run `npm run dev`. Visit `/ro`, `/ru`, `/en`. Hero subhead and section titles change per locale. Tagline stays as the Romanian brand line on all three.

---

## Task 4: `shop` namespace + Shop page and shop/* components

**Files (create):**
- `src/i18n/locales/ro/shop.json`
- `src/i18n/locales/ru/shop.json`
- `src/i18n/locales/en/shop.json`

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/Shop.tsx`
- `src/components/shop/FilterSidebar.tsx`
- `src/components/shop/ProductsView.tsx`
- `src/components/shop/BrandsView.tsx`

- [ ] **Step 1: Inventory strings**

Read all four files. List filter labels, sort option labels, empty-state copy, the "În stoc primele" sort, "Doar produse în stoc" filter, active filter chips, "Load more" button, brands-view labels, breadcrumbs.

- [ ] **Step 2: Populate `ro/shop.json`**

Skeleton — extend or trim per inventory:

```json
{
  "title": "Magazin",
  "tabs": {
    "products": "Produse",
    "brands": "Branduri"
  },
  "filters": {
    "title": "Filtre",
    "open": "Filtrează",
    "clear": "Șterge filtrele",
    "apply": "Aplică",
    "brand": "Brand",
    "gender": "Gen",
    "concentration": "Concentrație",
    "priceRange": "Preț",
    "inStockOnly": "Doar produse în stoc"
  },
  "gender": {
    "men": "Bărbați",
    "women": "Femei",
    "unisex": "Unisex"
  },
  "sort": {
    "label": "Sortează",
    "inStockFirst": "În stoc primele",
    "priceAsc": "Preț crescător",
    "priceDesc": "Preț descrescător",
    "newest": "Cele mai noi",
    "popular": "Populare"
  },
  "results": {
    "count_one": "{{count}} produs",
    "count_other": "{{count}} produse",
    "empty": "Niciun produs nu corespunde filtrelor.",
    "emptyHint": "Resetează filtrele sau încearcă alt brand.",
    "loadMore": "Încarcă mai multe"
  },
  "activeFilters": {
    "label": "Filtre active",
    "clearAll": "Șterge toate"
  },
  "brands": {
    "viewProducts": "Vezi produse",
    "empty": "Niciun brand disponibil."
  }
}
```

- [ ] **Step 3: Translate to `ru/shop.json` and `en/shop.json`**

Standard restrained voice. Plural forms: Russian uses `_one`, `_few`, `_many`, `_other` — use the i18next standard. English uses `_one` and `_other`.

For Russian plural example:
```json
"count_one": "{{count}} продукт",
"count_few": "{{count}} продукта",
"count_many": "{{count}} продуктов",
"count_other": "{{count}} продукта"
```

For English:
```json
"count_one": "{{count}} product",
"count_other": "{{count}} products"
```

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Add `shop` import and entries to `resources` and `ns`, mirroring Task 3 Step 4.

- [ ] **Step 5: Migrate Shop.tsx**

Read `src/pages/Shop.tsx`. `useTranslation('shop')`. Replace tab labels, page title, empty-state copy. Migrate `<Link>` / `navigate` to `useLocalizedHref()`.

- [ ] **Step 6: Migrate FilterSidebar.tsx**

Read `src/components/shop/FilterSidebar.tsx`. Replace section headings, checkbox labels, gender labels, sort options, "clear filters" button.

- [ ] **Step 7: Migrate ProductsView.tsx**

Read `src/components/shop/ProductsView.tsx`. Replace results count (use `t('results.count', { count })` for pluralization), empty state, load-more button, active filter chip labels.

- [ ] **Step 8: Migrate BrandsView.tsx**

Read `src/components/shop/BrandsView.tsx`. Replace `viewProducts` button, empty state. Migrate brand detail navigation to `useLocalizedHref()`.

- [ ] **Step 9: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 10: Smoke test**

Run `npm run dev`. Visit `/ro/shop`, `/ru/shop`, `/en/shop`. Filter labels change per locale. Plural form on results count is correct ("1 produs" vs "5 produse" in RO; "1 product" vs "5 products" in EN).

---

## Task 5: `product` namespace + Product page and product/* components

**Files (create):**
- `src/i18n/locales/ro/product.json`
- `src/i18n/locales/ru/product.json`
- `src/i18n/locales/en/product.json`

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/Product.tsx`
- `src/components/product/ProductImage.tsx`
- `src/components/product/SizeSelector.tsx`
- `src/components/product/PurchaseBlock.tsx`
- `src/components/product/MobileBuyBar.tsx`
- `src/components/product/NotesSection.tsx`
- `src/components/product/DetailsSection.tsx`
- `src/components/product/Rating.tsx`

- [ ] **Step 1: Inventory strings**

Read all listed files. Identify size-selector labels, "Adaugă în coș", "În stoc" / "În aprovizionare" badges, longevity/projection labels, notes section heading + pyramid labels (top/heart/base), details section labels (concentration, year, gender, perfumer), shipping estimate copy if rendered inline (note: ShippingEstimate component is migrated in Task 7), Rating fallback ("Nu există recenzii încă") if shown.

- [ ] **Step 2: Populate `ro/product.json`**

Skeleton:

```json
{
  "size": {
    "label": "Mărime",
    "ml": "ml",
    "decant": "decant"
  },
  "stock": {
    "inStock": "În stoc",
    "backorder": "În aprovizionare",
    "outOfStock": "Indisponibil"
  },
  "purchase": {
    "addToCart": "Adaugă în coș",
    "adding": "Se adaugă...",
    "added": "Adăugat",
    "buyNow": "Comandă acum",
    "selectSize": "Alege mărimea"
  },
  "notes": {
    "title": "Note",
    "top": "Note de vârf",
    "heart": "Note de mijloc",
    "base": "Note de bază"
  },
  "details": {
    "title": "Detalii",
    "concentration": "Concentrație",
    "year": "An",
    "gender": "Gen",
    "perfumer": "Parfumier",
    "country": "Țară",
    "family": "Familie olfactivă"
  },
  "rating": {
    "noReviews": "Nu există recenzii încă"
  },
  "share": {
    "label": "Distribuie",
    "copyLink": "Copiază link"
  }
}
```

- [ ] **Step 3: Translate to `ru/product.json` and `en/product.json`**

Restrained voice. "decant" stays as "decant" in EN; "декант" in RU; "decant" in RO. "ml" universal.

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Add `product` to imports, `resources`, `ns`.

- [ ] **Step 5: Migrate Product.tsx**

Read `src/pages/Product.tsx`. `useTranslation('product')` (and `'common'` if needed). Migrate any hardcoded RO. Migrate links via `useLocalizedHref()`.

- [ ] **Step 6: Migrate ProductImage.tsx**

Read `src/components/product/ProductImage.tsx`. Replace any aria-labels or alt-text fallback strings.

- [ ] **Step 7: Migrate SizeSelector.tsx**

Read `src/components/product/SizeSelector.tsx`. Replace size label, "ml" suffix if hardcoded, "decant" badge, stock indicator badges.

- [ ] **Step 8: Migrate PurchaseBlock.tsx**

Read `src/components/product/PurchaseBlock.tsx`. Replace add-to-cart button states, "Alege mărimea" hint, in-stock/backorder labels, share button.

- [ ] **Step 9: Migrate MobileBuyBar.tsx**

Read `src/components/product/MobileBuyBar.tsx`. Replace add-to-cart button states.

- [ ] **Step 10: Migrate NotesSection.tsx**

Read `src/components/product/NotesSection.tsx`. Replace section heading and pyramid level labels.

- [ ] **Step 11: Migrate DetailsSection.tsx**

Read `src/components/product/DetailsSection.tsx`. Replace section heading and field labels (Concentrație, An, Gen, etc.). NOTE: the field VALUES come from DB and remain in their stored language (typically RO) until A2 — only the LABELS are translated.

- [ ] **Step 12: Migrate Rating.tsx**

Read `src/components/product/Rating.tsx`. Replace any "no reviews" fallback string with `t('rating.noReviews')`.

- [ ] **Step 13: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 14: Smoke test**

Run `npm run dev`. Visit a product page in each locale: `/ro/product/<id>`, `/ru/product/<id>`, `/en/product/<id>`. Size selector, purchase block, notes section, details section labels all change per locale. Product name/brand/notes values stay in their DB language (expected).

---

## Task 6: `discovery` namespace + Discovery pages and discovery/* components

**Files (create):**
- `src/i18n/locales/ro/discovery.json`
- `src/i18n/locales/ru/discovery.json`
- `src/i18n/locales/en/discovery.json`

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/DiscoverySets.tsx`
- `src/pages/DiscoverySet.tsx`
- `src/components/discovery/PredefinedSetsGrid.tsx`
- `src/components/discovery/SetPurchaseBlock.tsx`
- `src/components/discovery/SetMobileBuyBar.tsx`
- `src/components/discovery/SetTray.tsx`
- `src/components/discovery/SetCatalogPane.tsx`
- `src/components/discovery/SetBuilder.tsx`
- `src/components/discovery/RecommendationWizard.tsx`

- [ ] **Step 1: Inventory strings**

Read all listed files. Identify view tabs ("Predefinite" / "Configurează" / "Recomandare"), set tile labels, builder pane labels (catalog filters, slot manager, "X / 7 selectate"), wizard step prompts and option labels (3 steps), purchase block strings, mobile buy bar.

- [ ] **Step 2: Populate `ro/discovery.json`**

Skeleton:

```json
{
  "title": "Seturi de descoperire",
  "subtitle": "Construiește-ți propriul set sau alege unul gata făcut.",
  "tabs": {
    "predefined": "Predefinite",
    "builder": "Configurează",
    "recommend": "Recomandare"
  },
  "predefined": {
    "empty": "Niciun set disponibil momentan.",
    "viewSet": "Vezi setul"
  },
  "builder": {
    "title": "Construiește setul",
    "subtitle": "Alege până la șapte parfumuri.",
    "slotsCount": "{{count}} / 7 selectate",
    "slotEmpty": "Slot liber",
    "slotRemove": "Șterge",
    "addToSet": "Adaugă în set",
    "remove": "Șterge",
    "catalog": {
      "search": "Caută parfum sau brand",
      "filterBrand": "Brand",
      "filterGender": "Gen",
      "empty": "Niciun rezultat. Încearcă alte filtre."
    },
    "summary": {
      "title": "Setul tău",
      "price": "Preț",
      "addToCart": "Adaugă setul în coș",
      "selectAtLeastOne": "Adaugă cel puțin un parfum"
    }
  },
  "recommend": {
    "title": "Recomandare",
    "subtitle": "Răspunde la trei întrebări scurte.",
    "step": "Pasul {{current}} din {{total}}",
    "next": "Mai departe",
    "back": "Înapoi",
    "submit": "Vezi recomandările",
    "results": {
      "title": "Setul recomandat",
      "subtitle": "Pe baza preferințelor tale.",
      "addToCart": "Adaugă în coș",
      "tryAgain": "Reia chestionarul"
    }
  },
  "set": {
    "includes": "Include",
    "ml": "ml",
    "decant": "decant",
    "addToCart": "Adaugă setul în coș",
    "back": "Înapoi la seturi"
  },
  "purchase": {
    "addToCart": "Adaugă setul în coș",
    "adding": "Se adaugă...",
    "added": "Adăugat"
  }
}
```

NOTE: Wizard step prompts and option labels live in DB if dynamic, or hardcoded if static. Read `RecommendationWizard.tsx` to confirm. If the prompts are hardcoded RO, add them under `recommend.steps.<stepId>.prompt` and `recommend.steps.<stepId>.options.<optionKey>`. If they come from DB, mark them as A2 deferred.

- [ ] **Step 3: Translate to `ru/discovery.json` and `en/discovery.json`**

Same restrained voice. "decant" / "декант" / "decant" pattern.

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Add `discovery` to imports, `resources`, `ns`.

- [ ] **Step 5: Migrate DiscoverySets.tsx**

Read `src/pages/DiscoverySets.tsx`. `useTranslation('discovery')`. Replace title, subtitle, tab labels. Migrate any internal links via `useLocalizedHref()`. The `?view=...` query param dispatch logic stays intact.

- [ ] **Step 6: Migrate DiscoverySet.tsx**

Read `src/pages/DiscoverySet.tsx`. Replace "Include", "Înapoi la seturi", any other static copy. Migrate links.

- [ ] **Step 7: Migrate PredefinedSetsGrid.tsx**

Read `src/components/discovery/PredefinedSetsGrid.tsx`. Replace "Vezi setul" CTA, empty state. Migrate set-detail link via `useLocalizedHref()`.

- [ ] **Step 8: Migrate SetPurchaseBlock.tsx and SetMobileBuyBar.tsx**

Read both files. Replace purchase block strings (add-to-cart states, includes label).

- [ ] **Step 9: Migrate SetTray.tsx**

Read `src/components/discovery/SetTray.tsx`. Replace "Slot liber", remove buttons, "X / 7 selectate" counter (use interpolation `t('builder.slotsCount', { count })`).

- [ ] **Step 10: Migrate SetCatalogPane.tsx**

Read `src/components/discovery/SetCatalogPane.tsx`. Replace search placeholder, brand/gender filter labels, empty state, "Adaugă în set" button.

- [ ] **Step 11: Migrate SetBuilder.tsx**

Read `src/components/discovery/SetBuilder.tsx`. Replace title, subtitle, summary section, "Adaugă setul în coș" button states.

- [ ] **Step 12: Migrate RecommendationWizard.tsx**

Read `src/components/discovery/RecommendationWizard.tsx`. Replace step counter ("Pasul X din Y"), navigation buttons, results-screen strings. If prompts are hardcoded, replace with `t('recommend.steps.<id>.prompt')`. If DB-sourced, leave values untouched and note as A2 deferred.

- [ ] **Step 13: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 14: Smoke test**

Run `npm run dev`. Visit `/ro/discovery-sets`, `/ru/discovery-sets`, `/en/discovery-sets`. Tab switches work. Click into Configurează → builder strings translate. Click into Recomandare → wizard strings translate. Click a predefined set → detail page strings translate.

---

## Task 7: `checkout` namespace + Checkout, forms, summary, MobileSubmitBar, ShippingEstimate components, country labels helper

**Files (create):**
- `src/i18n/locales/ro/checkout.json`
- `src/i18n/locales/ru/checkout.json`
- `src/i18n/locales/en/checkout.json`
- `src/utils/countries.ts` (new helper)

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/Checkout.tsx`
- `src/components/checkout/CheckoutForm.tsx`
- `src/components/checkout/OrderSummary.tsx`
- `src/components/checkout/MobileSubmitBar.tsx`
- `src/components/ShippingEstimate.tsx`
- `src/components/ShippingEstimateForCart.tsx`

- [ ] **Step 1: Inventory strings**

Read all listed files. Catalog: form section headings, every label, every placeholder, every Zod-derived validation message, summary section labels (subtotal, VAT, shipping, total), shipping estimate copy ("Livrare în {{min}}–{{max}} zile lucrătoare" / "În aprovizionare: {{min}}–{{max}}"), submit button states.

- [ ] **Step 2: Populate `ro/checkout.json`**

Skeleton:

```json
{
  "title": "Finalizare comandă",
  "form": {
    "sectionContact": "Date de contact",
    "sectionShipping": "Adresă de livrare",
    "sectionNotes": "Observații",
    "fullName": "Nume complet",
    "email": "Email",
    "phone": "Telefon",
    "address": "Adresa de livrare",
    "addressLine2": "Apartament / etaj (opțional)",
    "city": "Oraș",
    "postalCode": "Cod poștal",
    "country": "Țara",
    "notes": "Observații (opțional)",
    "notesPlaceholder": "Mențiuni pentru curier sau echipa noastră"
  },
  "validation": {
    "fullNameRequired": "Numele este obligatoriu",
    "emailRequired": "Emailul este obligatoriu",
    "emailInvalid": "Email invalid",
    "phoneRequired": "Telefonul este obligatoriu",
    "phoneInvalid": "Telefon invalid",
    "addressRequired": "Adresa este obligatorie",
    "cityRequired": "Orașul este obligatoriu",
    "postalCodeRequired": "Codul poștal este obligatoriu",
    "countryRequired": "Selectează țara"
  },
  "summary": {
    "title": "Sumar comandă",
    "subtotal": "Subtotal",
    "vat": "TVA ({{rate}}%)",
    "shipping": "Livrare",
    "total": "Total",
    "items_one": "{{count}} produs",
    "items_other": "{{count}} produse"
  },
  "shippingEstimate": {
    "inStock": "Livrare în {{min}}–{{max}} zile lucrătoare",
    "backorder": "În aprovizionare: {{min}}–{{max}} zile lucrătoare",
    "mixed": "Comanda conține produse în stoc și în aprovizionare. Livrare în {{min}}–{{max}} zile lucrătoare."
  },
  "submit": "Plasează comanda",
  "submitting": "Se procesează...",
  "contactNote": "Vom reveni pe WhatsApp sau Telegram pentru confirmare."
}
```

Adjust per the exact validation messages in the existing Zod schema.

- [ ] **Step 3: Translate to `ru/checkout.json` and `en/checkout.json`**

Russian uses `_one`, `_few`, `_many`, `_other` for `items` plural. Match the same shape used in Task 4.

- [ ] **Step 4: Create `src/utils/countries.ts`**

```ts
import type { TFunction } from 'i18next';
import { EU_COUNTRIES } from './vat';

export function getCountryLabel(code: string, t: TFunction): string {
  const key = `countries.${code}`;
  const translated = t(key, { ns: 'common' });
  return translated === key ? code : translated;
}

export function getLocalizedCountryOptions(t: TFunction) {
  return EU_COUNTRIES.map((country) => ({
    code: country.code,
    label: getCountryLabel(country.code, t),
    vatRate: country.vatRate,
  })).sort((a, b) => a.label.localeCompare(b.label));
}
```

NOTE: This helper returns the existing `EU_COUNTRIES` shape from `vat.ts` with localized labels. Verify the property names against `vat.ts` and adjust the spread accordingly (e.g., if `vatRate` is named differently, use that). Moldova ("MD") is also a valid country option — if `vat.ts` keeps Moldova separate, prepend `{ code: 'MD', label: getCountryLabel('MD', t), vatRate: 0 }` to the returned list.

- [ ] **Step 5: Register namespace in `src/i18n/index.ts`**

Add `checkout` to imports, `resources`, `ns`.

- [ ] **Step 6: Migrate Checkout.tsx**

Read `src/pages/Checkout.tsx`. `useTranslation('checkout')`. Replace title, navigation, contact note. Migrate any internal links.

- [ ] **Step 7: Migrate CheckoutForm.tsx**

Read `src/components/checkout/CheckoutForm.tsx`. Replace section headings, all field labels, placeholders. Update Zod schema validation messages: where the schema used `"Numele este obligatoriu"`, switch to `t('validation.fullNameRequired')`. The Zod message must be a function of `t`, so wrap the schema in a factory:

```ts
import { z } from 'zod';
import type { TFunction } from 'i18next';

export function makeCheckoutSchema(t: TFunction) {
  return z.object({
    fullName: z.string().min(1, t('validation.fullNameRequired')),
    email: z.string().email(t('validation.emailInvalid')),
    // ... etc
  });
}
```

Inside the form component, call the factory with the live `t` from `useTranslation('checkout')`.

- [ ] **Step 8: Migrate the country `<Select>` in CheckoutForm.tsx**

Replace the country options data source with `getLocalizedCountryOptions(t)`. Pass the `t` from `useTranslation('common')` (since the country labels live in `common`). Sort by localized label.

- [ ] **Step 9: Migrate OrderSummary.tsx**

Read `src/components/checkout/OrderSummary.tsx`. Replace summary section labels: subtotal, VAT (with rate interpolation), shipping, total, items count (with pluralization).

- [ ] **Step 10: Migrate MobileSubmitBar.tsx**

Read `src/components/checkout/MobileSubmitBar.tsx`. Replace submit button states.

- [ ] **Step 11: Migrate ShippingEstimate.tsx and ShippingEstimateForCart.tsx**

Read both files. Replace "Livrare în {{min}}–{{max}} zile lucrătoare" and the backorder/mixed variants with `t('shippingEstimate.<key>', { min, max })`. Keep the `getShippingEstimate` logic from `src/utils/shippingEstimate.ts` untouched — only the rendered strings change.

- [ ] **Step 12: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 13: Smoke test**

Run `npm run dev`. Visit `/ro/checkout`, `/ru/checkout`, `/en/checkout`. Form labels, validation messages (trigger by submitting empty form), summary block, shipping estimate, submit button all change per locale. Country dropdown shows localized country names sorted by localized label. Submit a valid form and verify the success path still works.

---

## Task 8: `order` namespace + OrderConfirmation (dual-mode page)

**Files (create):**
- `src/i18n/locales/ro/order.json`
- `src/i18n/locales/ru/order.json`
- `src/i18n/locales/en/order.json`

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/OrderConfirmation.tsx`

- [ ] **Step 1: Inventory strings**

Read `src/pages/OrderConfirmation.tsx`. The page has two modes: `?placed=1` (just-placed celebration) and bare URL (returning customer detail view). Catalog every string for both modes. Also catalog status strings if they're rendered (status values likely come from DB enum → translate the LABELS, leave VALUES as-is or map enum-to-key).

- [ ] **Step 2: Populate `ro/order.json`**

Skeleton:

```json
{
  "celebration": {
    "title": "Mulțumim pentru comandă",
    "subtitle": "Vom reveni pe WhatsApp sau Telegram în scurt timp pentru confirmare.",
    "orderNumber": "Numărul comenzii: {{id}}",
    "continueShopping": "Continuă cumpărăturile"
  },
  "detail": {
    "title": "Detalii comandă",
    "orderNumber": "Comanda #{{id}}",
    "placedAt": "Plasată pe {{date}}",
    "status": "Stare",
    "items": "Produse",
    "summary": "Sumar",
    "shippingTo": "Livrare la",
    "contactSupport": "Pentru asistență, contactează-ne pe WhatsApp sau Telegram."
  },
  "status": {
    "pending": "În așteptare",
    "confirmed": "Confirmată",
    "processing": "În procesare",
    "shipped": "Expediată",
    "delivered": "Livrată",
    "cancelled": "Anulată"
  },
  "summary": {
    "subtotal": "Subtotal",
    "vat": "TVA",
    "shipping": "Livrare",
    "total": "Total"
  }
}
```

Verify status enum values against DB / `Database` types and adjust keys to match the actual enum strings. If the enum uses different identifiers, rename the keys accordingly.

- [ ] **Step 3: Translate to `ru/order.json` and `en/order.json`**

Match conventions.

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Add `order` to imports, `resources`, `ns`.

- [ ] **Step 5: Migrate OrderConfirmation.tsx**

Read `src/pages/OrderConfirmation.tsx`. `useTranslation('order')`. Replace strings in both modes. Status badge labels via `t('status.${order.status}')`. Date formatting: use `Intl.DateTimeFormat(i18n.language, { dateStyle: 'long' }).format(date)` for locale-aware date strings, replacing any hardcoded RO date format. Migrate "Continuă cumpărăturile" link via `useLocalizedHref()`.

- [ ] **Step 6: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 7: Smoke test**

Run `npm run dev`. Place a test order to land on `/ro/orders/<id>?placed=1`. Switch language; celebration copy translates. Visit `/ro/orders/<id>` (no `?placed=1`); detail mode renders. Status badge label translates. Date format reads naturally per locale.

---

## Task 9: `static` namespace + About, Contact, FAQ, Privacy, Terms, Careers, Login, NotFound

**Files (create):**
- `src/i18n/locales/ro/static.json`
- `src/i18n/locales/ru/static.json`
- `src/i18n/locales/en/static.json`

**Files (modify):**
- `src/i18n/index.ts` — register namespace
- `src/pages/About.tsx`
- `src/pages/Contact.tsx`
- `src/pages/FAQ.tsx`
- `src/pages/Privacy.tsx`
- `src/pages/Terms.tsx`
- `src/pages/Careers.tsx`
- `src/pages/Login.tsx`
- `src/pages/NotFound.tsx`

- [ ] **Step 1: Inventory strings**

Read all eight files. Static pages are dense — FAQ and Terms are likely the largest. Use heading + paragraph keys. Long blocks of legal/FAQ copy go under nested keys like `faq.q1.question` / `faq.q1.answer` or `terms.section1.title` / `terms.section1.body`.

- [ ] **Step 2: Populate `ro/static.json`**

Skeleton — extend significantly per inventory. Per-page top-level key, then nested by section/heading:

```json
{
  "about": {
    "title": "Despre",
    "lead": "...",
    "sections": {
      "story": { "title": "Povestea", "body": "..." },
      "mission": { "title": "Misiune", "body": "..." }
    }
  },
  "contact": {
    "title": "Contact",
    "lead": "Suntem aici pentru întrebări, recomandări sau orice altceva.",
    "whatsapp": "WhatsApp",
    "telegram": "Telegram",
    "email": "Email",
    "address": "Adresă"
  },
  "faq": {
    "title": "Întrebări frecvente",
    "items": [
      { "question": "...", "answer": "..." }
    ]
  },
  "privacy": {
    "title": "Politica de confidențialitate",
    "lastUpdated": "Ultima actualizare: {{date}}",
    "sections": {
      "intro": { "title": "...", "body": "..." }
    }
  },
  "terms": {
    "title": "Termeni și condiții",
    "lastUpdated": "Ultima actualizare: {{date}}",
    "sections": {
      "intro": { "title": "...", "body": "..." }
    }
  },
  "careers": {
    "title": "Cariere",
    "lead": "...",
    "openings": "Posturi deschise",
    "noOpenings": "Nu avem posturi deschise momentan."
  },
  "login": {
    "title": "Autentificare",
    "email": "Email",
    "password": "Parolă",
    "submit": "Autentifică-te",
    "submitting": "Se autentifică...",
    "forgotPassword": "Ai uitat parola?",
    "createAccount": "Creează cont",
    "errorInvalid": "Email sau parolă incorecte"
  },
  "notFound": {
    "title": "Pagină negăsită",
    "body": "Pagina pe care o cauți nu există sau a fost mutată.",
    "backHome": "Înapoi acasă"
  }
}
```

For FAQ and Terms with N items/sections, prefer a structured array (FAQ) or nested object (Terms) keyed by stable identifiers. Don't fabricate copy that doesn't exist in the source — extract only what's there.

- [ ] **Step 3: Translate to `ru/static.json` and `en/static.json`**

Long-form translation. Maintain register: legal text stays formal; FAQ stays conversational but restrained.

- [ ] **Step 4: Register namespace in `src/i18n/index.ts`**

Add `static` to imports, `resources`, `ns`.

- [ ] **Step 5: Migrate each page (8 separate steps, one per page)**

For each of the 8 pages:
1. `useTranslation('static')`
2. Replace every hardcoded RO string with `t('<page>.<key>')`
3. Migrate any `<Link>` / `navigate` to `useLocalizedHref()` (mostly relevant for NotFound's back-home link and Login's redirect)

The Login page's auth flow logic (Supabase `signInWithPassword`, error handling) stays untouched — only the rendered copy changes.

- [ ] **Step 6: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 7: Smoke test**

Run `npm run dev`. Visit each page in each locale. FAQ items render correctly. Terms / Privacy long-form copy renders. Login form translates. NotFound back-home link routes to the right localized home.

---

## Task 10: SEO meta — usePageMeta hook + per-page wiring + meta keys in all namespaces

**Files (create):**
- `src/hooks/usePageMeta.ts`

**Files (modify):**
- All 8 namespace JSON files in all 3 locales (add `meta` block to each)
- All 15 customer-facing pages (call `usePageMeta` from each)

- [ ] **Step 1: Create usePageMeta hook**

Write `src/hooks/usePageMeta.ts`:

```tsx
import { Helmet } from 'react-helmet-async';
import { useLocation } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { SUPPORTED_LANGS, DEFAULT_LANG } from '@/i18n';

const SITE_BASE_URL = 'https://modestshop.md';
const SITE_NAME = 'ModestShop';

const OG_LOCALE: Record<string, string> = {
  ro: 'ro_MD',
  ru: 'ru_RU',
  en: 'en_US',
};

interface PageMetaProps {
  titleKey: string;
  descriptionKey: string;
  namespace: string;
  values?: Record<string, string | number>;
}

function stripLang(pathname: string): string {
  const stripped = pathname.replace(/^\/[^/]+/, '');
  return stripped || '/';
}

export function PageMeta({ titleKey, descriptionKey, namespace, values }: PageMetaProps) {
  const { t, i18n } = useTranslation(namespace);
  const location = useLocation();
  const lang = i18n.language;
  const pathnameLangless = stripLang(location.pathname);

  const title = t(titleKey, values ?? {});
  const description = t(descriptionKey, values ?? {});
  const canonical = `${SITE_BASE_URL}/${lang}${pathnameLangless}`;

  return (
    <Helmet>
      <title>{`${title} · ${SITE_NAME}`}</title>
      <meta name="description" content={description} />
      <meta property="og:title" content={`${title} · ${SITE_NAME}`} />
      <meta property="og:description" content={description} />
      <meta property="og:locale" content={OG_LOCALE[lang] ?? OG_LOCALE[DEFAULT_LANG]} />
      <link rel="canonical" href={canonical} />
      {SUPPORTED_LANGS.map((l) => (
        <link
          key={l}
          rel="alternate"
          hrefLang={l}
          href={`${SITE_BASE_URL}/${l}${pathnameLangless}`}
        />
      ))}
      <link
        rel="alternate"
        hrefLang="x-default"
        href={`${SITE_BASE_URL}/${DEFAULT_LANG}${pathnameLangless}`}
      />
    </Helmet>
  );
}
```

(Exposing as a component, not a hook, since `<Helmet>` itself is a component. Naming: `PageMeta`. Consumers render `<PageMeta titleKey="meta.title" descriptionKey="meta.description" namespace="shop" />` near the top of each page.)

- [ ] **Step 2: Add `meta` block to every namespace, every locale**

For each of `home`, `shop`, `product`, `discovery`, `checkout`, `order`, `static` (note: `common` does not need `meta` — it's chrome, not pages), add a `meta` block.

For static-namespace pages with multiple page sub-namespaces (about/contact/faq/privacy/terms/careers/login/notFound), nest the meta under each page key:

```json
{
  "about": { "meta": { "title": "Despre", "description": "..." }, ... },
  "contact": { "meta": { "title": "Contact", "description": "..." }, ... },
  // ...
}
```

For the others, top-level `meta`:

```json
{
  "meta": { "title": "Magazin", "description": "..." },
  // ... existing keys
}
```

For PDP and Discovery-set detail pages where the title is a function of DB content, use interpolation:

```json
"meta": {
  "title": "{{name}} — {{brand}}",
  "description": "{{name}} de {{brand}}. Decanturi de la 2ml. Livrare în Moldova și UE."
}
```

Translate every meta string into RU and EN. Keep titles short (40-60 chars), descriptions ~150 chars.

- [ ] **Step 3: Wire `<PageMeta>` on every customer-facing page**

For each of the 15 pages, add the component at the top of the rendered tree:

| Page | namespace | titleKey | descriptionKey | values |
|---|---|---|---|---|
| `Index.tsx` | `home` | `meta.title` | `meta.description` | — |
| `Shop.tsx` | `shop` | `meta.title` | `meta.description` | — |
| `Product.tsx` | `product` | `meta.title` | `meta.description` | `{ name, brand }` |
| `DiscoverySets.tsx` | `discovery` | `meta.title` | `meta.description` | — |
| `DiscoverySet.tsx` | `discovery` | `meta.titleSet` | `meta.descriptionSet` | `{ name }` |
| `Checkout.tsx` | `checkout` | `meta.title` | `meta.description` | — |
| `OrderConfirmation.tsx` | `order` | `meta.title` | `meta.description` | — |
| `About.tsx` | `static` | `about.meta.title` | `about.meta.description` | — |
| `Contact.tsx` | `static` | `contact.meta.title` | `contact.meta.description` | — |
| `FAQ.tsx` | `static` | `faq.meta.title` | `faq.meta.description` | — |
| `Privacy.tsx` | `static` | `privacy.meta.title` | `privacy.meta.description` | — |
| `Terms.tsx` | `static` | `terms.meta.title` | `terms.meta.description` | — |
| `Careers.tsx` | `static` | `careers.meta.title` | `careers.meta.description` | — |
| `Login.tsx` | `static` | `login.meta.title` | `login.meta.description` | — |
| `NotFound.tsx` | `static` | `notFound.meta.title` | `notFound.meta.description` | — |

For DiscoverySet detail, add a separate set of keys `meta.titleSet` and `meta.descriptionSet` to `discovery.json` with interpolation for `{{name}}`. The Discovery landing keeps its base `meta.title` / `meta.description`.

Example wiring in `Product.tsx`:

```tsx
import { PageMeta } from '@/hooks/usePageMeta';

// inside component, after data is loaded:
{product && (
  <PageMeta
    namespace="product"
    titleKey="meta.title"
    descriptionKey="meta.description"
    values={{ name: product.name, brand: product.brand_name }}
  />
)}
```

Render `<PageMeta>` only when the interpolation values are available. For pages without interpolation, render unconditionally.

- [ ] **Step 4: Verify build**

Run: `npm run build`
Expected: success.

- [ ] **Step 5: Smoke test**

Run `npm run dev`. For each page in each locale:
- View page source (or DevTools `<head>`)
- Verify `<title>` includes the translated page title and `· ModestShop`
- Verify `<meta name="description">` is the translated description
- Verify `<html lang>` matches the URL prefix
- Verify three `<link rel="alternate" hreflang="...">` entries exist (one per supported lang) plus one `x-default`
- Verify `<link rel="canonical">` URL matches the current locale's URL

For PDP, verify `{{name}}` and `{{brand}}` interpolate into the title.

---

## Task 11: Final verification + raw-string sweep

- [ ] **Step 1: Final build**

Run: `npm run build`
Expected: success.

- [ ] **Step 2: Sweep for un-migrated hardcoded RO strings**

Run from project root:

```bash
grep -rn "[ÂâÎîȘșȚțĂă]" src/pages/ src/components/ --include="*.tsx" --include="*.ts" \
  | grep -v "^src/components/admin/" \
  | grep -v "^src/pages/Admin" \
  | grep -v "^Binary file" \
  | grep -v "// " \
  | grep -v "^.*://[^\"]"
```

This catches any remaining customer-facing strings with Romanian-specific characters. Review each match:
- If it's a translation key value inside a `.json` file: skip
- If it's an `aria-label` / `placeholder` / button label / text content in JSX: migrate
- If it's a comment: leave
- If it's an admin-adjacent file the grep didn't filter: leave

If matches that should be migrated remain, migrate them and re-run.

- [ ] **Step 3: Sweep for un-localized internal links**

Run:

```bash
grep -rn "to=\"/" src/pages/ src/components/ --include="*.tsx" \
  | grep -v "^src/components/admin/" \
  | grep -v "^src/pages/Admin" \
  | grep -v "to=\"//"
```

Every match in customer-facing code should be migrated to `useLocalizedHref()` unless it's a deliberately-bare path (e.g., a redirect to login outside the localized tree, which shouldn't exist after this work).

```bash
grep -rn "navigate(\"/" src/pages/ src/components/ --include="*.tsx" --include="*.ts" \
  | grep -v "^src/components/admin/" \
  | grep -v "^src/pages/Admin"
```

Same review pattern.

- [ ] **Step 4: Check Vercel redirect for SEO transition**

Read `vercel.json` (if it exists). Add 301 redirects for the legacy unprefixed URLs to preserve SEO from existing organic traffic:

```json
{
  "redirects": [
    { "source": "/shop", "destination": "/ro/shop", "permanent": true },
    { "source": "/product/:id", "destination": "/ro/product/:id", "permanent": true },
    { "source": "/discovery-sets", "destination": "/ro/discovery-sets", "permanent": true },
    { "source": "/discovery-set/:id", "destination": "/ro/discovery-set/:id", "permanent": true },
    { "source": "/checkout", "destination": "/ro/checkout", "permanent": true },
    { "source": "/about", "destination": "/ro/about", "permanent": true },
    { "source": "/contact", "destination": "/ro/contact", "permanent": true },
    { "source": "/faq", "destination": "/ro/faq", "permanent": true },
    { "source": "/privacy", "destination": "/ro/privacy", "permanent": true },
    { "source": "/terms", "destination": "/ro/terms", "permanent": true },
    { "source": "/careers", "destination": "/ro/careers", "permanent": true },
    { "source": "/login", "destination": "/ro/login", "permanent": true }
  ]
}
```

If `vercel.json` already exists, merge the `redirects` array — don't overwrite other config.

NOTE: The bare-root redirect inside `App.tsx` already handles `/`, but server-side 301s are stronger SEO signals than client-side `<Navigate>` and prevent the unprefixed URLs from competing for ranking with the localized versions.

- [ ] **Step 5: Final smoke test in all three locales**

Run `npm run dev`. Walk through one full happy path per locale:
- Land on `/<lang>` (Home)
- Navigate to `/<lang>/shop`
- Open a product
- Add to cart
- Open cart sheet → click checkout
- Fill checkout form → trigger validation → submit
- Land on order confirmation

For each step, confirm:
- All visible strings are in the active locale
- Internal links keep the locale prefix
- Language switcher swaps between RO/RU/EN preserving the current page

- [ ] **Step 6: Hand off to user for full QA**

The implementation is complete. The user runs full QA themselves: hover states, edge cases, RTL/LTR (no RTL languages here so skip), screen reader sanity, mobile-specific surfaces, admin verification (admin pages should still render in Romanian regardless of URL prefix).

---

## Self-review notes (plan author)

- **Spec coverage:**
  - Library stack (i18next + react-i18next + react-helmet-async): Task 1 Step 1 ✓
  - i18n config with fallbackLng=ro, defaultNS=common: Task 1 Step 2 ✓
  - Translation file layout (8 namespaces × 3 locales): Tasks 2-9 ✓
  - Routing with `/:lang/*` parent + bare-root redirect + admin under `/:lang/admin`: Task 1 Step 7 ✓
  - LanguageGate validates lang and sets `<html lang>`: Task 1 Step 5 ✓
  - useLocalizedHref hook: Task 1 Step 4 ✓
  - LanguageSwitcher with header/footer variants: Task 2 Step 6 ✓
  - SEO meta with hreflang including x-default: Task 10 Step 1 ✓
  - per-page meta on every customer-facing page: Task 10 Step 3 ✓
  - Country labels in common.json + helper: Task 2 Step 2 + Task 7 Step 4 ✓
  - Pluralization (RO/RU/EN forms): Task 4 Step 3, Task 7 Step 3 ✓
  - Vercel 301 redirects for SEO transition: Task 11 Step 4 ✓
  - Admin stays Romanian: Task 1 Step 7 (route renders `<Admin>` regardless of `:lang` content) — admin code is not in any migration task, which keeps it RO ✓

- **Placeholder scan:** No "TBD" / "TODO" / "implement later" patterns. Each migration task lists files explicitly and provides a key skeleton with concrete strings. Translation steps say "translate from RO" — this is a directive, not a placeholder, because the implementer is expected to perform the translation given the brand voice notes and the RO source.

- **Type consistency:**
  - `PageMeta` (component, not hook) is consistent across Task 10
  - `useLocalizedHref()` returns `(path: string) => string` — used consistently in every migration task
  - `LanguageGate` exported via named export `{ LanguageGate }`, imported as such in App.tsx
  - `SUPPORTED_LANGS`, `DEFAULT_LANG`, `isSupportedLang`, `SupportedLang` exported from `src/i18n/index.ts` and imported by `LanguageGate`, `useLocalizedHref`, `LanguageSwitcher`, `PageMeta`
  - i18next namespace registration: every migration task adds the new namespace to `src/i18n/index.ts` `resources` and `ns` array

- **Scope check:** Single sub-project (A1 + A3). No multi-subsystem split needed.

- **Per user preference:** No QA task. Task 11 Step 6 is a hand-off, not a QA task.
